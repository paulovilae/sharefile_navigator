# Python dependencies
pytesseract
# For GPU OCR
# torch (if using GPU)
easyocr

# System dependencies (must be installed separately):
# - tesseract-ocr (required for pytesseract)
#   On Ubuntu: sudo apt-get install tesseract-ocr
#   On Windows: Download from https://github.com/tesseract-ocr/tesseract and add to PATH 