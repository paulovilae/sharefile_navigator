/**
 * Legacy BatchStatusDisplay - now imports from the modular batch-processing folder
 * This file maintains backward compatibility while using the new modular structure
 */
import BatchStatusDisplay from '../batch-processing/components/shared/BatchStatusDisplay';

export default BatchStatusDisplay;